{% set name = "cw2" %}
{% set version = "2.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/cw2-{{ version }}.tar.gz
  sha256: 6d74c5a97c946be8ebc395da9a4e2fd249933ac1c485aeeb712a1e19bba877dc

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - setuptools >=61.0
    - pip
  run:
    - python >=3.6
    - pyyaml
    - numpy
    - pandas
    - joblib

test:
  imports:
    - cw2
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/ALRhub/cw2
  summary: A reengineered framework to run experiments on a computing cluster.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - YYYasin19
